services:
  - type: web
    name: flask-intro
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn run:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.5
      - key: FLASK_DEBUG
        value: false
